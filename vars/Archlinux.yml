---
# role: podman
# file: vars/Archlinux.yml

__podman_packages:
  - podman
  - shadow
  - fuse-overlayfs

__podman_validators:
  containers.conf: /usr/bin/env CONTAINERS_CONF=%s /usr/bin/podman --help
  storage.conf: /usr/bin/env CONTAINERS_STORAGE_CONF=%s /usr/bin/podman --help
